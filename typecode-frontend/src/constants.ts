export enum STORES {
  AUTH_TYPECODE = "auth",
  CHAT_TYPECODE = "chat"
}

export enum PAGE_PATHS {
  LOCAL = "http://localhost:3000",
  SIGNUP = "/signup",
  LOGIN = "/login",
  HOME = "/home",
  CHAT = "/chat/:id",
  ROOM = "/room/:id"
}
